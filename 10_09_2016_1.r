demo()
help()
3+2
#aedf
install.packages("Rcmdr", dependencies=TRUE)
q()
source("D:/учеба/пзтв/Z1.r" echo=T)
source("D:/учеба/пзтв/Z1.r", echo=T)
source("D:/учеба/пзтв/Z1.r", echo=T)
source("D:/учеба/пзтв/Z1.r", echo=T)
var(a)
var(x)
x
results<-c(length(x),mean(x),median(x),var(x),sqrt(var(a)),min(x),max(x),max(x)-min(x))
results
length(x)
mean(x_
;
mean(x)
median(x)
var(a)
sqrt(var(a))
max(x)
min(x)
results<-c(length(x),mean(x),median(x),var(x),sqrt(var(a)),min(x),max(x),max(x)-min(x))
results<-c(length(x),mean(x),median(x),var(a),sqrt(var(a)),min(x),max(x),max(x)-min(x))
results
name(results)<-c("Объем выборки","Среднее", "Медиана", "Дисперсия", "Станд.отклонение", "Минимум", "Максимум", "Размах")
names(results)<-c("Объем выборки","Среднее", "Медиана", "Дисперсия", "Станд.отклонение", "Минимум", "Максимум", "Размах")
results
d<-dataframe(result=results)
d<-data.frame(result=results)
d
write.table(d,file="D:/учеба/пзтв/Z1.txt")
write.table(d,file="D:/учеба/пзтв/Z1.xls")
write.table(d,file
=
;
write.table(d,file="D:/учеба/пзтв/Z1.xls",quote=F,col.names=NA)
write.table(results,file="D:/учеба/пзтв/Z1.xls",quote=F,col.names=NA)
write.table(results,file="D:/учеба/пзтв/Z1.txt",quote=F,col.names=NA)
save.history(file="D:/учеба/пзтв/10_09_2016_1.r
")
savehistory(file="D:/учеба/пзтв/10_09_2016_1.r")
